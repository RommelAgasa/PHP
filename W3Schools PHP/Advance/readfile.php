<!DOCTYPE html>
<html>
<body>

    <?php
        // The readfile() function is useful if all you want to 
        // do is open up a file and read its contents.
        // echo readfile("dic.txt");
        
        // $myfile = fopen('dic.txt', 'r') or die("Unable to open a file.");
        // echo fread($myfile, filesize('dic.txt'));
        // fclose($myfile);

        // $myfile = fopen('Brgy1.docx', 'r') or die("Unable to open a file.");

        // Ouput one line until end-of-file
        // while(!feof($myfile)){
        //     echo fgets($myfile) . "<br>";
        // }

        // fclose($myfile);

    ?>

</body>
</html>